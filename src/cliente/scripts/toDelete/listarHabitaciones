/*async function fetchItems() {
            const response = await fetch('getItems.php');
            const items = await response.json();
            const container = document.getElementById('scrollContainer');
            
            items.forEach(item => {
                const div = document.createElement('div');
                div.className = 'item';
                div.textContent = item;
                container.appendChild(div);
            });
        }

        // Ejecuta la función para obtener los datos
        fetchItems();

        // JavaScript para controlar el desplazamiento con la rueda del ratón
        document.getElementById('scrollContainer').addEventListener('wheel', function(e) {
            e.preventDefault(); // Evita el desplazamiento de la página
            this.scrollTop += e.deltaY; // Desplaza el contenedor
        });*/